[workspace]
members = [
    "vajra-engine",
    "vajra-protocol",
    "vajra-daemon",
    "vajra-cli",
    "vajra-ui-tauri/src-tauri",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
