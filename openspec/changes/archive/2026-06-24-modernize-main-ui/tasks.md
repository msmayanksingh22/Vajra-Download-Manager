# Tasks

- [x] Create Fission-AI spec for UI modernization.
- [x] Fix `index.css` by mapping `--color-bg1`, `--color-bg2`, `--color-bg3`, `--color-bg4` to the correct variables so Tailwind picks them up.
- [x] Ensure `OptionsDialog.jsx` implements the save function correctly (verified).
- [x] Build the UI so the executable has the latest changes.
