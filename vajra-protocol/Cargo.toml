[package]
name = "vajra-protocol"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
dirs-next = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
utoipa = "4"
uuid = { version = "1", features = ["serde", "v4"] }
rand = "0.8"
hex = "0.4"

[dev-dependencies]
tempfile = "3"
